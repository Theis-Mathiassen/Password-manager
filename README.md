# Password storage
A password manager written in C#, using windows forms.

It is using AES-256 encryption to store the passwords encrypted, in a local file.

The source code, can be found on the desktop version branch.
